
      const tickCount = Math.max(5, Math.floor((yMax - yMin) / (yMax - yMin) * 6));

      // Dinamik tick aralıkları
      const yTicks = d3.range(yMin, yMax, (yMax - yMin) / tickCount);
      console.log(yTicks)
