 onFavoriteChange={handleRemoveFavoriteApp} // Home'deki işlemi StreamCard'a ilet

 const handleAddFavorite = async () => {
    const newStatus = !favStatus;
    setFavStatus(newStatus);

    // Home'deki işlemi tetikleyerek favorileri güncelle
    await onFavoriteChange(id);
  };
