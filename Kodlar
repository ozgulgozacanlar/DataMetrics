  const [loaded, setLoaded] = useState(false); // Verinin bir kez yüklenip yüklenmediğini kontrol edeceğiz.
    if (!loaded && app) {
        setLoaded(true); // İlk yükleme tamamlandığında loaded'ı true yapıyoruz
