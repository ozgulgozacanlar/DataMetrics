  const x0 = d3.scaleBand().rangeRound([0, width]).padding(0.1); // Kategori grubu için scale
  const x1 = d3.scaleBand().padding(0.05); // Grup içindeki çubuklar için scale
  const y = d3.scaleLinear().rangeRound([height, 0]);
