 useEffect(() => {
    // _app nesnesinin nasıl erişileceği burada belirlenmelidir
    // _app'in geçerli ve önceden yüklenmiş bir nesne olduğundan emin olun.

    // Parametrelerin her birini dinamik şekilde işliyoruz
    Object.entries(params).forEach(([key, value]) => {
      // Dinamik parametre ismi ile selectMatch çağrısı yapıyoruz
      _app.field(key).selectMatch(value).then(() => {
        console.log(`${key} parametresi seçildi: ${value}`);
      });
    });
  }, [params]); // params state'i değiştiğinde bu useEffect tetiklenecek
