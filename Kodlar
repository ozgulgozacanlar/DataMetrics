const tickCount = Math.max(5, Math.floor((yMax - yMin) / 200));  // Y eksenindeki adım büyüklüğü
const step = Math.round((yMax - yMin) / tickCount / 10) * 10; // Adım büyüklüğünü yuvarla

const yTicks = d3.range(yMin, yMax, step);  // Y eksenindeki tam sayılarla oluşan tick aralığı
