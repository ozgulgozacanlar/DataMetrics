    const yScale = d3.scaleBand()
        .domain(data.slice(0, visibleCount).map(d => d.name))
        .range([margin.top, margin.top + (heightPerBar + barPadding) * visibleCount])
        .padding(0);

    const barPadding = 5; // Sabit boşluk değeri
