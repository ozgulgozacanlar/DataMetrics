 .attr("fill", (d) => {
    // Global sayaç ile her barı sırasıyla renkli yapıyoruz
    const color = barCounter % 2 === 0 ? "#2c84cc" : "#a4c48c";  // Alternatif renkler (Mavi ve Yeşil)
    barCounter++;  // Sayaç arttırılıyor
    return color;
  });

let barCounter = 0; // Global sayaç
