   const secondArray = measureTitles.map((headerItem, idx) => {
                                const values = response.map((item => item[idx + 1].qText));
                                return [...values];
                            });
