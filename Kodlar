    .ticks(Math.round(upperLimit / 0.2))  // Tick sayısını hesaplıyoruz
    .tickValues(d3.range(0, upperLimit + 0.2, 0.2))  // 0 ile upperLimit arasında 0.2 aralıklarla değerler
    .tickFormat(d3.format('.1f'));  // 1 ondalıklı formatta görünsün
