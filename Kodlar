 // Y ekseninin domain değerini en büyük `qNum` değerine göre ayarlıyoruz
  const maxYValue = d3.max(data, d => d3.max(d.slice(1), e => e.qNum));
  const y = d3.scaleLinear().rangeRound([height, 0]).domain([0, maxYValue]);
