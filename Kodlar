 const filteredApps = qlikTableArray.flatMap(item =>
      item.apps.filter(app =>
        isMatch(app.appName, searchTerm) &&
        app.appName.toLowerCase().includes(searchTerm.toLowerCase()) // Tam eşleşme ve arama için bu kontrolü koruyoruz.
      )
    )  
