const yScale = d3.scaleBand()
    .domain(data.slice(0, visibleCount).map(d => d.name)) // Görünür öğeleri tanımlamak için
    .range([margin.top, margin.top + heightPerBar * visibleCount]) // Sabit aralıklar kullan
    .padding(0.2); // Görünümün düzgün görünmesi için padding kullanılabilir
