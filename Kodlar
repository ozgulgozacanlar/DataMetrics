Card: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. 

              <Card key={`${card.objectId}-${rndKey}`} objectId={card.objectId} app={app} title={card.title} />

export default function Card({ key, app, objectId, title }) {
    const [data, setData] = useState();

    useEffect(() => {
        if (app) {
            getObjectData(app, objectId)
                .then((response) => {
                    setData(response[0])
                    if(objectId == "vHfhH"){
                        console.log(response)
                    }
                    // console.log(`Qlik Sense object ${objectId} rendered successfully.`);
                })
                .catch((error) => {
                    console.error('Error rendering Qlik Sense object:', error);
                });
        } else {
            console.error('Qlik Sense app is not loaded yet');
        }
    }, [app, objectId, key]);
