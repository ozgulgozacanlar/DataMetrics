 const x = d3.scaleBand()
    .domain(dataBar.map(d => parseDate(translateMonthToEnglish(d.date))))  // Her bir tarihi domena ekliyoruz
    .range([0, width])  // Eksenin genişliği
    .padding(0.1);  // Barlar arasına boşluk bırakıyoruz
