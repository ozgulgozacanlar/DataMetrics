 <table className="low-high-table modal-table">
            <thead>
                <tr>                  
                    <th>&nbsp;</th>
                    {tableTitleArray && tableTitleArray.map((title, index) => (
                        <th colSpan={5} key={index}>
                            {title}
                        </th>
                    ))}
