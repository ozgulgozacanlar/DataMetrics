 <div className="card-header d-flex justify-content-between align-items-center">
                    {/* Card Title Component */}
                    <CardTitle title="Bölge ve Banka Verileri" />

                    {/* Tab'lar Sağ Tarafta */}
                    <div className="tab-options">
                        <span
                            className={`tab-option ${selectedOption === 'region' ? 'active' : ''}`}
                            onClick={() => handleClick('region')}
                        >
                            Bölge
                        </span>
                        <span
                            className={`tab-option ${selectedOption === 'bank' ? 'active' : ''}`}
                            onClick={() => handleClick('bank')}
                        >
                            Banka
                        </span>
                    </div>
                </div>
.card-header {
    display: flex;
    justify-content: space-between; /* Sol ve sağ arasındaki alanı eşit olarak dağıt */
    align-items: center;
    padding: 10px 20px;
}

.card-title {
    flex: 1; /* Başlık sola yerleşsin, sağdaki alanı bırak */
}

.tab-options {
    display: flex;
    gap: 20px;
}

.tab-option {
    cursor: pointer;
    padding: 10px 20px;
    border: 1px solid #ccc;
    border-radius: 5px;
    transition: background-color 0.3s ease;
}

.tab-option:hover {
    background-color: #f0f0f0;
}

.tab-option.active {
    background-color: #007bff;
    color: white;
    font-weight: bold;
    border-color: #007bff;
}
