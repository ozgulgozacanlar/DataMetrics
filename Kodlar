  setCardTitles((prevTitles) => [
          ...prevTitles,
          { title: titles, objectId }  // Store both title and objectId
        ]);

    onChange(newValue, selectedObjectId);  // Send both the title and objectId
